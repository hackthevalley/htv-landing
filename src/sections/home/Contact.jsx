import React from 'react';
import styled from 'styled-components';
import { Container as C } from '@components';

export const Contact = () => <Container tag="section">Contact</Container>;

const Container = styled(C)``;
