import React from 'react';
import styled from 'styled-components';
import { Container as C } from '@components';

export const Schedule = () => <Container tag="section">Schedule</Container>;

Schedule.nav = {
    path: '/#schedule',
    name: 'Schedule',
};

const Container = styled(C)``;
